<Window x:Class="KenthSP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kenth_sSP"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Border Background="Gray" Padding="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Background="LightGray" Grid.Column="0" Margin="0,0,10,0" Padding="10" CornerRadius="0,50,0,0">
                <StackPanel>
                    <TextBlock Text="Study Planner" FontWeight="Bold" FontSize="20" Margin="0,0,0,80"/>
                    <Button x:Name="TodayButton" Content="Today" Height="40" Margin="0,0,0,20" Click="TodayButton_Click"/>
                    <Button Content="Sujects" Height="40" Margin="0,0,0,20"/>
                    <Button Content="Task" Height="40" Margin="0,0,0,20"/>
                    <Button Content="Progress"  Height="40" Margin="0,0,0,20"/>
                </StackPanel>
            </Border>

            <Grid Grid.Column="1" Background="LightGray" Width="500" Height="300" Margin="0,90,0,0">
                <ContentControl x:Name="MainContent" Content="MainContent"/>
            </Grid>
        </Grid>
    </Border>
</Window>
